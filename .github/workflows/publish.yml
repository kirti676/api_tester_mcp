name: Publish Package

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main, master]

permissions:
  contents: write  # Required for creating releases
  packages: write  # Required for publishing packages

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Install Node.js dependencies
        run: npm install --ignore-scripts

      - name: Run tests
        run: |
          # Add test commands here when available
          echo "Tests would run here"

      - name: Check if NPM version exists
        id: npm-check
        run: |
          set +e
          npm view @kirti676/api-tester-mcp@$(node -p "require('./package.json').version") > /dev/null 2>&1
          if [ $? -eq 0 ]; then
            echo "version_exists=true" >> $GITHUB_OUTPUT
            echo "📦 Package version already exists on NPM"
          else
            echo "version_exists=false" >> $GITHUB_OUTPUT
            echo "🚀 Package version does not exist, will publish"
          fi
          set -e

      - name: Publish to npm
        if: steps.npm-check.outputs.version_exists == 'false'
        run: |
          echo "🚀 Publishing to NPM..."
          echo "NPM_TOKEN is set: ${{ secrets.NPM_TOKEN != '' }}"
          npm publish --access public
          echo "✅ Successfully published to NPM!"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Skip NPM publish (version exists)
        if: steps.npm-check.outputs.version_exists == 'true'
        run: echo "⏭️  Skipping NPM publish - version already exists"

  publish-pypi:
    runs-on: ubuntu-latest
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check if PyPI version exists
        id: pypi-check
        run: |
          set +e
          PKG_VERSION=$(python -c "exec(open('api_tester_mcp/__init__.py').read()); print(__version__)")
          pip index versions api-tester-mcp 2>/dev/null | grep -q "$PKG_VERSION"
          if [ $? -eq 0 ]; then
            echo "version_exists=true" >> $GITHUB_OUTPUT
            echo "Package version already exists on PyPI"
          else
            echo "version_exists=false" >> $GITHUB_OUTPUT
            echo "Package version does not exist, will publish"
          fi
          set -e

      - name: Publish to PyPI
        if: steps.pypi-check.outputs.version_exists == 'false'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          
      - name: Skip PyPI publish (version exists)
        if: steps.pypi-check.outputs.version_exists == 'true'
        run: echo "Skipping PyPI publish - version already exists"

  create-release:
    runs-on: ubuntu-latest
    needs: [publish-npm, publish-pypi]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## Changes
            - See commit history for detailed changes
            
            ## Installation
            ```bash
            # npm
            npm install -g @kirti676/api-tester-mcp
            
            # pip
            pip install api-tester-mcp
            ```
            
            ## Quick Start
            ```bash
            npx @kirti676/api-tester-mcp --setup
            ```
          draft: false
          prerelease: false
          fail_on_unmatched_files: false
